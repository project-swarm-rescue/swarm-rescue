-- main flow of the bot

wifi.setmode(wifi.STATION)
sta_cfg ={}

sta_cfg.ssid =''
--random movement avoiding obstacles(looking for an ap )

--far_away_alg(once ap is found -> rssi>-60)

--near_alg(rssi>-60 to rssi>-40, stop at a distance and locate)

--position information publishing(setup its own ap)

